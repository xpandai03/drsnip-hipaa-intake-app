# Zapier Catch Hook URL — receives the form submission payload server-side.
# Required for /api/submit to work. Set to a webhook.site URL for local payload inspection,
# or to the staging/production Zapier hook for end-to-end testing.
ZAPIER_WEBHOOK_URL=
